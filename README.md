# LightTrack

本仓库保留两个硬件平台的第一版代码基线：

- `ss927/`：当前主开发分支，包含最新的 `LightTracker` 功能实现。
- `dv500/`：当前 DV500 平台基线；尚未同步 `ss927/` 后续新增的跟踪、Pose/PTZ 输出等功能改动。

因此，新的功能设计、问题定位和实现优先以 `ss927/` 为准。向 `dv500/` 同步时，应按独立平台适配任务进行代码比较、编译与设备 replay 验证，不能假定两端可直接复制。
